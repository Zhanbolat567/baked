# 📋 Production Deployment Checklist

Используйте этот чек-лист перед развертыванием приложения в production.

## 🔐 Безопасность

### Backend
- [ ] **Изменен SECRET_KEY** в `.env`
  ```bash
  # Сгенерируйте новый ключ:
  openssl rand -hex 32
  ```

- [ ] **Обновлены Kaspi API credentials**
  - [ ] `KASPI_API_KEY` - реальный ключ
  - [ ] `KASPI_API_SECRET` - реальный секрет
  - [ ] `KASPI_MERCHANT_ID` - реальный merchant ID

- [ ] **Установлен DEBUG=False** в `.env`

- [ ] **Настроены CORS_ORIGINS** только для вашего домена
  ```env
  CORS_ORIGINS=https://yourdomain.com
  ```

- [ ] **Изменены учетные данные администратора**
  - [ ] Телефон изменен с `+77001234567`
  - [ ] Пароль изменен с `admin123`
  - [ ] Используется сильный пароль (12+ символов, буквы, цифры, спецсимволы)

- [ ] **Изменены credentials базы данных PostgreSQL**
  ```env
  DATABASE_URL=postgresql://secure_user:strong_password@db:5432/social_db
  ```
  И в `docker-compose.yml`:
  ```yaml
  environment:
    POSTGRES_USER: secure_user
    POSTGRES_PASSWORD: strong_password
  ```

### Frontend
- [ ] **REACT_APP_API_URL** указывает на production backend
  ```env
  REACT_APP_API_URL=https://api.yourdomain.com
  ```

- [ ] **Удалены console.log** и debug код
  ```bash
  # Поиск console.log
  grep -r "console.log" frontend/src/
  ```

## 🌐 SSL/HTTPS

- [ ] **Получен SSL сертификат**
  - [ ] Используется Let's Encrypt (бесплатно)
  - [ ] Или куплен коммерческий сертификат

- [ ] **Настроен Nginx для HTTPS**
  ```nginx
  server {
      listen 443 ssl http2;
      ssl_certificate /etc/ssl/certs/your-cert.crt;
      ssl_certificate_key /etc/ssl/private/your-key.key;
      # ... остальная конфигурация
  }
  ```

- [ ] **Настроен редирект HTTP → HTTPS**
  ```nginx
  server {
      listen 80;
      return 301 https://$host$request_uri;
  }
  ```

- [ ] **Backend принимает HTTPS запросы**

## 📦 База данных

- [ ] **Настроены регулярные бэкапы PostgreSQL**
  ```bash
  # Пример cron задачи для ежедневного бэкапа
  0 2 * * * docker exec social_db pg_dump -U social_user social_db > /backups/social_db_$(date +\%Y\%m\%d).sql
  ```

- [ ] **Проверена репликация** (если используется)

- [ ] **Установлены лимиты подключений**
  ```yaml
  # docker-compose.yml
  command: postgres -c max_connections=200
  ```

- [ ] **Настроена персистентность данных**
  ```yaml
  volumes:
    - ./postgres_data:/var/lib/postgresql/data
  ```

## 🖼️ Медиа файлы

- [ ] **Загружены все изображения продуктов**
  - [ ] Оптимизированы для web (< 200KB каждое)
  - [ ] Все в формате JPG/PNG/WebP
  - [ ] Размеры изображений: 800x600px

- [ ] **Настроено хранилище uploads**
  ```yaml
  volumes:
    - ./uploads:/app/uploads
  ```

- [ ] **Проверены права доступа**
  ```bash
  chmod 755 backend/uploads
  ```

## ⚡ Производительность

- [ ] **Redis настроен и работает**
  - [ ] Кэширование меню активно
  - [ ] TTL кэша установлен (например, 1 час)

- [ ] **Nginx gzip сжатие включено**
  ```nginx
  gzip on;
  gzip_types text/plain text/css application/json application/javascript;
  ```

- [ ] **Frontend собран в production режиме**
  ```bash
  npm run build
  ```

- [ ] **Минимизированы bundle sizes**
  ```bash
  # Проверьте размер бандлов
  cd frontend
  npm run build
  # Проверьте frontend/build/static/js/
  ```

- [ ] **Database индексы созданы**
  ```sql
  CREATE INDEX idx_products_category ON products(category_id);
  CREATE INDEX idx_orders_status ON orders(status);
  CREATE INDEX idx_orders_user ON orders(user_id);
  ```

## 🔄 CI/CD

- [ ] **Настроен Git repository**
  ```bash
  git init
  git add .
  git commit -m "Initial commit"
  ```

- [ ] **Добавлен .gitignore**
  ```
  .env
  __pycache__/
  node_modules/
  postgres_data/
  uploads/
  *.pyc
  build/
  ```

- [ ] **Настроен pipeline** (GitHub Actions / GitLab CI / Jenkins)

- [ ] **Автоматизировано тестирование**

## 🧪 Тестирование

- [ ] **Проведено unit тестирование**
  ```bash
  cd backend
  pytest tests/
  ```

- [ ] **Проведено E2E тестирование**
  - [ ] Регистрация пользователя
  - [ ] Добавление в корзину
  - [ ] Оформление заказа
  - [ ] Админ вход
  - [ ] CRUD операции в админке

- [ ] **Проверена интеграция с Kaspi**
  - [ ] Создание инвойса
  - [ ] Проверка статуса оплаты
  - [ ] Обработка успешной оплаты
  - [ ] Обработка отмененной оплаты

- [ ] **Load testing** (опционально)
  ```bash
  # Пример с Apache Bench
  ab -n 1000 -c 100 https://api.yourdomain.com/api/v1/menu/
  ```

## 📊 Мониторинг

- [ ] **Настроены логи**
  - [ ] Backend логи сохраняются
  - [ ] Frontend errors tracked (Sentry / LogRocket)

- [ ] **Настроен health check**
  ```bash
  curl https://api.yourdomain.com/health
  ```

- [ ] **Мониторинг сервера** (Grafana / Datadog / New Relic)

- [ ] **Алерты настроены**
  - [ ] CPU > 80%
  - [ ] RAM > 90%
  - [ ] Disk > 85%
  - [ ] API response time > 2s

## 🚀 Deployment

- [ ] **Выбрана платформа хостинга**
  - [ ] VPS (DigitalOcean, Hetzner, AWS EC2)
  - [ ] PaaS (Heroku, Railway, Render)
  - [ ] Kubernetes (GKE, EKS, AKS)

- [ ] **Настроен domain и DNS**
  - [ ] A record: `yourdomain.com` → `server-ip`
  - [ ] A record: `www.yourdomain.com` → `server-ip`
  - [ ] A record: `api.yourdomain.com` → `server-ip`

- [ ] **Firewall настроен**
  ```bash
  # Только необходимые порты
  ufw allow 22/tcp    # SSH
  ufw allow 80/tcp    # HTTP
  ufw allow 443/tcp   # HTTPS
  ufw enable
  ```

- [ ] **Docker Compose в production режиме**
  ```yaml
  # docker-compose.prod.yml
  services:
    backend:
      restart: always
      logging:
        driver: "json-file"
        options:
          max-size: "10m"
          max-file: "3"
  ```

- [ ] **Автозапуск Docker при перезагрузке**
  ```bash
  sudo systemctl enable docker
  ```

## 📱 Функциональность

### Клиентская часть
- [ ] Регистрация работает
- [ ] Вход работает
- [ ] Меню загружается
- [ ] Фильтры по категориям работают
- [ ] Модальное окно продукта открывается
- [ ] Выбор размера работает
- [ ] Выбор опций работает
- [ ] Добавление в корзину работает
- [ ] Корзина показывает правильный итог
- [ ] Изменение количества в корзине работает
- [ ] Удаление из корзины работает
- [ ] QR код генерируется
- [ ] Статус оплаты обновляется
- [ ] Бонусные баллы отображаются
- [ ] История заказов работает

### Админ панель
- [ ] Вход в админку работает
- [ ] Dashboard показывает статистику
- [ ] Создание категории работает
- [ ] Редактирование категории работает
- [ ] Удаление категории работает
- [ ] Создание продукта работает
- [ ] Редактирование продукта работает
- [ ] Удаление продукта работает
- [ ] Управление опциями работает
- [ ] Список активных заказов работает
- [ ] Завершение заказа работает
- [ ] История заказов работает

## 📄 Документация

- [ ] **README.md актуален**

- [ ] **API документация доступна**
  ```
  https://api.yourdomain.com/docs
  ```

- [ ] **Инструкции для команды подготовлены**
  - [ ] Как добавить новый продукт
  - [ ] Как обработать заказ
  - [ ] Что делать при ошибках

- [ ] **Контактные данные для поддержки**

## 🆘 Rollback план

- [ ] **Подготовлен план отката**
  ```bash
  # Бэкап текущей версии
  docker-compose down
  tar -czf backup_$(date +%Y%m%d).tar.gz .
  
  # Откат к предыдущей версии
  git checkout previous-stable-tag
  docker-compose up -d
  ```

- [ ] **Протестирован процесс отката**

## ✅ Final Checks

После прохождения всех пунктов выше:

- [ ] **Проведен pre-launch тест** на тестовом окружении
- [ ] **Получено одобрение** от заказчика/владельца бизнеса
- [ ] **Команда проинформирована** о запуске
- [ ] **План поддержки** на первые 24-48 часов

---

## 🎉 Готово к запуску!

Когда все галочки поставлены, можно запускать в production:

```bash
# 1. На сервере
git clone https://github.com/your-repo/social-coffee.git
cd social-coffee

# 2. Настройте .env файлы
nano backend/.env
nano frontend/.env

# 3. Запустите
docker-compose -f docker-compose.prod.yml up -d

# 4. Инициализируйте БД
docker exec -it social_backend python seed_db.py

# 5. Проверьте
curl https://yourdomain.com
curl https://api.yourdomain.com/health

# 6. Мониторьте логи первые часы
docker-compose logs -f
```

**🎊 Поздравляем с запуском!**

## 📞 Post-Launch

После запуска:
- Следите за метриками первые 24 часа
- Собирайте обратную связь от пользователей
- Фиксируйте баги в issue tracker
- Планируйте hotfixes при необходимости
