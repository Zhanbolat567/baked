# Social Coffee Shop Web Application

Веб-приложение для кофейни "Social" с клиентской частью и административной панелью.

## 🏗️ Архитектура проекта

```
project/
├── backend/              # FastAPI Backend
│   ├── app/
│   │   ├── api/         # API endpoints
│   │   ├── core/        # Конфигурация, security
│   │   ├── db/          # База данных
│   │   ├── models/      # SQLAlchemy модели
│   │   ├── schemas/     # Pydantic схемы
│   │   └── services/    # Бизнес-логика (Kaspi API)
│   ├── Dockerfile
│   └── requirements.txt
│
├── frontend/            # React Frontend
│   ├── public/
│   ├── src/
│   │   ├── components/  # React компоненты
│   │   ├── pages/       # Страницы
│   │   ├── services/    # API сервисы
│   │   ├── store/       # State management (Zustand)
│   │   └── types/       # TypeScript типы
│   ├── Dockerfile
│   ├── nginx.conf
│   └── package.json
│
└── docker-compose.yml   # Orchestration
```

## 🚀 Быстрый старт

### Требования
- Docker & Docker Compose
- (Опционально) Node.js 18+ и Python 3.11+ для разработки

### Запуск за 5 минут

1. **Установите зависимости frontend**:
```powershell
cd frontend
npm install
cd ..
```

2. **Создайте файлы `.env`**:
```powershell
# Backend
cp backend\.env.example backend\.env

# Frontend
cp frontend\.env.example frontend\.env
```

> 📍 **2GIS Карты**: Для работы визуализации зон доставки получите бесплатный API ключ на [dev.2gis.com](https://dev.2gis.com/) и добавьте его в `frontend/.env`:
> ```
> VITE_2GIS_API_KEY=ваш_api_ключ
> ```
> Подробная инструкция в [2GIS_MAPS_SETUP.md](./2GIS_MAPS_SETUP.md)

3. **Запустите Docker Compose**:
```powershell
docker-compose up --build
```

4. **Инициализируйте базу данных**:
```powershell
# В новом окне терминала
docker exec -it social_backend python seed_db.py
```

5. **Откройте в браузере**:
- 🌐 Клиентская часть: http://localhost:3000
- 📚 API документация: http://localhost:8000/docs
- 👑 Админ панель: http://localhost:3000/admin

**Данные для входа (администратор):**
- 📱 Телефон: `+7 (700) 123-45-67`
- 🔑 Пароль: `admin123`

> ⚠️ **Важно:** Измените эти данные перед развертыванием в production!

### Подробная инструкция

См. [SETUP.md](./SETUP.md) для детальной инструкции по настройке и развертыванию.

## 📋 Основные функции

### Клиентская часть
- ✅ Просмотр меню (категории и товары)
- ✅ Кастомизация товаров (выбор опций)
- ✅ Корзина покупок
- ✅ Аутентификация пользователей
- ✅ Оплата через Kaspi QR
- ✅ Накопление бонусов
- ✅ Многоязычность (Русский, Казахский, English)

### Админ панель
- ✅ Dashboard с статистикой продаж
- ✅ Управление категориями (CRUD)
- ✅ Управление товарами (CRUD)
- ✅ Управление опциями/модификаторами
- ✅ Просмотр активных и закрытых заказов
- ✅ Изменение статуса заказов
- ✅ Визуализация зон доставки на 2GIS картах
- ✅ Рисование и редактирование зон доставки

### Backend (API)
- ✅ RESTful API на FastAPI
- ✅ JWT аутентификация
- ✅ PostgreSQL для хранения данных
- ✅ Redis для кэширования меню
- ✅ Интеграция с Kaspi Bank API
- ✅ Автоматическая инвалидация кэша

## 🔧 Разработка

### Backend разработка

```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

### Frontend разработка

```bash
cd frontend
npm install
npm start
```

## 🗄️ База данных

### Создание администратора

Для создания пользователя с ролью администратора, выполните SQL:

```sql
INSERT INTO users (first_name, last_name, phone_number, password_hash, role)
VALUES ('Admin', 'User', '+77001234567', '<hashed_password>', 'admin');
```

Или используйте API endpoint `/auth/register` и затем измените роль в базе данных.

### Миграции

Проект использует SQLAlchemy. Для применения изменений схемы:

```bash
cd backend
alembic revision --autogenerate -m "Description"
alembic upgrade head
```

## 🔐 Безопасность

- ✅ HTTPS (настроить SSL сертификаты в production)
- ✅ JWT токены для аутентификации
- ✅ CORS настроен для определенных доменов
- ✅ Защита admin endpoints (требуется роль администратора)
- ✅ Хеширование паролей (bcrypt)

## 📡 API Endpoints

### Публичные
- `POST /api/v1/auth/register` - Регистрация
- `POST /api/v1/auth/login` - Вход
- `GET /api/v1/menu` - Получение меню (с кэшированием)
- `POST /api/v1/orders` - Создание заказа
- `GET /api/v1/orders/status/{order_id}` - Статус оплаты

### Требуют авторизации
- `GET /api/v1/orders/{order_id}` - Детали заказа

### Admin (требуется роль admin)
- `GET /api/v1/admin/dashboard` - Статистика
- `GET /api/v1/admin/orders/active` - Активные заказы
- `GET /api/v1/admin/orders/closed` - Закрытые заказы
- `PATCH /api/v1/admin/orders/{id}/complete` - Завершить заказ
- `GET/POST/PUT/DELETE /api/v1/admin/categories` - Управление категориями
- `GET/POST/PUT/DELETE /api/v1/admin/products` - Управление товарами
- `GET/POST /api/v1/admin/option-groups` - Управление группами опций
- `POST/DELETE /api/v1/admin/options` - Управление опциями

## 🧪 Тестирование

### Backend тесты
```bash
cd backend
pytest
```

### Frontend тесты
```bash
cd frontend
npm test
```

## 📦 Deployment

### Production сборка

1. Настройте environment variables в `.env` файлах
2. Настройте SSL сертификаты
3. Запустите production build:

```bash
docker-compose -f docker-compose.prod.yml up --build -d
```

### Рекомендации для Production
- Используйте реальный HTTPS с Let's Encrypt или другим SSL провайдером
- Настройте резервное копирование PostgreSQL
- Используйте managed Redis (например, Redis Cloud)
- Настройте мониторинг и логирование
- Используйте .env файлы с реальными секретными ключами
- Настройте CI/CD pipeline

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

Proprietary - Social Coffee Shop

## 👥 Контакты

Для вопросов и поддержки: support@social.kz

---

**Версия:** 1.0.0  
**Дата:** 06.11.2025  
**Статус:** Production Ready
