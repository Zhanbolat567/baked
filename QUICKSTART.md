# 🚀 Быстрый старт Social Coffee Shop

## Шаг 1: Установка frontend зависимостей

```powershell
cd frontend
npm install
cd ..
```

## Шаг 2: Настройка переменных окружения

### Backend (.env)
```powershell
# Скопируйте пример
copy backend\.env.example backend\.env

# Или создайте вручную со следующими значениями:
DATABASE_URL=postgresql://social_user:social_password@db:5432/social_db
REDIS_URL=redis://cache:6379/0
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Kaspi API (опционально для первого запуска)
KASPI_API_KEY=your-kaspi-api-key
KASPI_API_SECRET=your-kaspi-secret-key
KASPI_MERCHANT_ID=your-merchant-id

DEBUG=True
CORS_ORIGINS=http://localhost:3000,http://localhost:8000
```

### Frontend (.env)
```powershell
# Скопируйте пример
copy frontend\.env.example frontend\.env

# Или создайте вручную:
REACT_APP_API_URL=http://localhost:8000
REACT_APP_NAME=Social Coffee Shop
REACT_APP_VERSION=2.0.0
```

## Шаг 3: Запуск Docker Compose

```powershell
docker-compose up --build
```

**Дождитесь запуска всех сервисов:**
- ✅ PostgreSQL (db) - health check passed
- ✅ Redis (cache) - health check passed
- ✅ Backend (backend) - Running on http://0.0.0.0:8000
- ✅ Frontend (frontend) - Nginx serving on port 3000

## Шаг 4: Инициализация базы данных

**В новом окне PowerShell:**

```powershell
docker exec -it social_backend python seed_db.py
```

**Вы увидите:**
```
Creating admin user...
✓ Admin user created
Creating option groups...
✓ Created 3 option groups
Creating categories...
✓ Created 9 categories
Creating products...
✓ Created 6 products
Database seeded successfully!
```

## Шаг 5: Проверка работы

### 🌐 Откройте в браузере:

1. **Клиентская часть** - http://localhost:3000
   - Должно отображаться меню с продуктами
   - Попробуйте добавить товар в корзину
   
2. **API документация** - http://localhost:8000/docs
   - Интерактивная документация Swagger UI
   - Можете тестировать эндпоинты
   
3. **Админ панель** - http://localhost:3000/admin
   - Войдите с данными:
     - 📱 **Телефон**: `+7 (700) 123-45-67`
     - 🔑 **Пароль**: `admin123`

## 📝 Тестовый сценарий

### A) Клиентская часть (http://localhost:3000)

1. **Регистрация нового пользователя:**
   - Нажмите "Войти" в верхнем правом углу
   - Переключитесь на вкладку "Регистрация"
   - Введите: телефон `+7 (777) 555-11-22`, имя `Тест`, пароль `test123`
   - Нажмите "Зарегистрироваться"

2. **Добавление товара в корзину:**
   - Нажмите на любой продукт (например, "Латте")
   - Выберите размер: `450ml` (+200₸)
   - Выберите молоко: `Coconut` (+400₸)
   - Добавьте сироп: `Caramel` (+300₸)
   - Увеличьте количество до 2
   - Нажмите "Добавить в корзину"

3. **Оформление заказа:**
   - Нажмите на иконку корзины (должен показываться суммарный чек)
   - Проверьте выбранные опции и количество
   - Нажмите "Оформить заказ"
   - Увидите QR-код для оплаты через Kaspi

4. **Симуляция оплаты (mock):**
   - В реальном сценарии клиент сканирует QR-код
   - Сейчас работает mock (через 15 секунд статус автоматически изменится)
   - После "успешной оплаты" корзина очистится

### B) Админ панель (http://localhost:3000/admin)

1. **Вход в админку:**
   - Откройте http://localhost:3000/admin
   - Введите: `+7 (700) 123-45-67` / `admin123`
   - Нажмите "Войти"

2. **Дашборд:**
   - Должны видеть статистику продаж (пока 0₸)
   - После тестовых заказов здесь появятся данные

3. **Управление меню:**
   - Перейдите на вкладку "Меню"
   - (Будут добавлены интерфейсы для CRUD операций)

4. **Управление заказами:**
   - Перейдите на вкладку "Заказы"
   - (Будет список активных заказов для обработки)

## 🔍 Проверка API

### Тестирование через Swagger UI (http://localhost:8000/docs)

1. **Регистрация пользователя:**
   - POST `/api/v1/auth/register`
   - Body:
     ```json
     {
       "phone_number": "+77771234567",
       "first_name": "Test User",
       "password": "testpass123"
     }
     ```

2. **Получение меню:**
   - GET `/api/v1/menu/`
   - Должен вернуть 9 категорий с продуктами

3. **Авторизация (для защищенных эндпоинтов):**
   - POST `/api/v1/auth/login`
   - Body:
     ```json
     {
       "username": "+77771234567",
       "password": "testpass123"
     }
     ```
   - Скопируйте `access_token`
   - Нажмите кнопку "Authorize" вверху страницы
   - Введите: `Bearer <ваш_токен>`

## 🛠️ Полезные команды

### Просмотр логов
```powershell
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend
```

### Остановка и перезапуск
```powershell
# Остановить все контейнеры
docker-compose down

# Перезапустить с пересборкой
docker-compose up --build

# Остановить и удалить volumes (сброс БД)
docker-compose down -v
```

### Доступ к контейнерам
```powershell
# Backend shell
docker exec -it social_backend bash

# PostgreSQL
docker exec -it social_db psql -U social_user -d social_db

# Redis CLI
docker exec -it social_cache redis-cli
```

### Проверка статуса
```powershell
# Список запущенных контейнеров
docker-compose ps

# Использование ресурсов
docker stats
```

## ❓ Troubleshooting

### Проблема: Порт 3000 или 8000 занят

```powershell
# Найти процесс на порту 3000
netstat -ano | findstr :3000

# Убить процесс (замените <PID> на реальный ID)
taskkill /PID <PID> /F
```

### Проблема: Frontend не может подключиться к backend

1. Проверьте, что backend запущен:
   ```powershell
   docker-compose ps backend
   ```

2. Проверьте `.env` в frontend:
   ```
   REACT_APP_API_URL=http://localhost:8000
   ```

3. Пересоберите frontend:
   ```powershell
   docker-compose up --build frontend
   ```

### Проблема: "Cannot find module 'react'"

Это нормально до выполнения `npm install`. Выполните:
```powershell
cd frontend
npm install
```

### Проблема: База данных пустая

Запустите seed script:
```powershell
docker exec -it social_backend python seed_db.py
```

### Проблема: Redis connection failed

Проверьте, что Redis запущен:
```powershell
docker-compose ps cache
docker-compose logs cache
```

## 📋 Следующие шаги

После успешного запуска, вам нужно:

### 1. 🔐 Изменить админ-данные

**Вариант A - через seed_db.py:**
```powershell
# Остановите контейнеры
docker-compose down

# Отредактируйте backend/seed_db.py
# Измените строки 14-16:
#   phone_number="+77001234567"  # ← Ваш телефон
#   password="admin123"           # ← Ваш пароль

# Пересоздайте БД и запустите
docker-compose up -d db cache
docker-compose up --build backend
docker exec -it social_backend python seed_db.py
```

**Вариант B - через SQL:**
```powershell
docker exec -it social_db psql -U social_user -d social_db

UPDATE users 
SET phone_number = '+77771234567', 
    password_hash = '$2b$12$...'  # Используйте bcrypt
WHERE role = 'ADMIN';
```

### 2. 🖼️ Добавить изображения продуктов

**Вариант A - загрузить свои:**
1. Поместите изображения в `backend/uploads/`
2. Обновите БД:
   ```sql
   UPDATE products SET image_url = '/uploads/latte.jpg' WHERE name = 'Латте';
   ```

**Вариант B - использовать с maidah.kz:**
```powershell
# Скрипт для скачивания из menu.json
docker exec -it social_backend python scripts/fetch_images.py
```

### 3. 💳 Настроить Kaspi API

Отредактируйте `backend/.env`:
```env
KASPI_API_KEY=your-real-api-key
KASPI_API_SECRET=your-real-secret
KASPI_MERCHANT_ID=your-merchant-id
```

Затем обновите `backend/app/services/kaspi.py` с реальными API вызовами.

### 4. 🏗️ Завершить админ-интерфейсы

- CRUD для категорий
- CRUD для продуктов
- CRUD для опций
- Обработка активных заказов

См. подробности в [SETUP.md](./SETUP.md)

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте [SETUP.md](./SETUP.md) - раздел Troubleshooting
2. Просмотрите логи: `docker-compose logs -f`
3. Проверьте статус контейнеров: `docker-compose ps`

---

**Готово! Приложение должно работать на http://localhost:3000** 🎉
